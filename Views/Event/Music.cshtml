@model List<EventDeneme.Models.EventCardViewModel>
@{ 
    Layout = "~/Views/Event/Eventpages.cshtml";
    ViewBag.Title = "Music";
}

@if (Model != null && Model.Any())
{
    @Html.Partial("_EventCards", Model)
}
else
{
    <p style="padding: 20px;">No concerts found.</p>
}


